# vim: set sw=4 sta et :

channel: lj
ntupledir: test/leptonplusjets
outdir: test/sync_mu

lumi: 10000

plot:
  - ttH2bb_125

cuts:
  - "Trigger": "event.trigger().single_mu()"
  - "Single μ": "leptons[0].muon() and len(leptons) == 1"
  - "≥ 4 jets": "len(jets) >= 4"
  - "≥ 2 tags": "len(filter(btag, jets)) >= 2"

weights:
  - Generator
