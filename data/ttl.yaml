# vim: set sw=4 sta et :

ntupledir: test
outdir: delete_me

lumi: 10000

plot:
  - ttjets
  - ttH_125
  - ttH_125_real

cuts:
  - "# leptons = 1": "leptons.size() == 1"
  - "Opposite charge for τ": "taus[0].charge() * taus[1].charge() < 0"
