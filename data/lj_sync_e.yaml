# vim: set sw=4 sta et :

channel: lj
ntupledir: test/leptonplusjets
outdir: test/sync_e

lumi: 10000

plot:
  - ttH2bb_125

cuts:
  - "Trigger": "event.trigger().single_e()"
  - "Single e": "leptons[0].electron() and len(leptons) == 1"
  - "≥ 4 jets": "len(jets) >= 4"
  # - "≥ 2 tags": "len(tags) >= 2"

weights:
  - Generator
